services:
  postgres:
    image: postgres:16-alpine
    shm_size: "512mb"
    container_name: chat-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - chat-network
    deploy:
      resources:
        limits:
          memory: 1G
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

  migration:
    build:
      context: .
      dockerfile: vikashsurin/chat-backend:latest
    container_name: chat-migration
    restart: no
    environment:
      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
    networks:
      - chat-network
    depends_on:
      postgres:
        condition: service_healthy
    command: ["bun", "run", "db:migrate"]

  backend:
    build:
      context: .
      dockerfile: vikashsurin/chat-backend:latest
    container_name: chat-backend
    restart: always
    depends_on:
      migration:
        condition: service_completed_successfully
    environment:
      - PORT=3000
      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
    networks:
      - chat-network
    command: ["bun", "run", "start"]

  frontend:
    build:
      context: .
      dockerfile: vikashsurin/chat-frontend:latest
    container_name: chat-frontend
    restart: always
    environment:
      - PORT=3001
      - ORIGIN=https://pingxy.com
      - INTERNAL_BACKEND_URL=http://backend:3000
    networks:
      - chat-network

  nginx:
    image: nginx:alpine
    container_name: chat-proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    networks:
      - chat-network
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/nginx/certs:ro # Mount the certs here
    depends_on:
      - frontend
      - backend

networks:
  chat-network:
    driver: bridge

volumes:
  postgres_data:
