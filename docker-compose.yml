services:
  postgres:
    image: postgres:16-alpine
    shm_size: "512mb"
    container_name: pingxy-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - pingxy-network
    deploy:
      resources:
        limits:
          memory: 1G
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

  migration:
    image: vikashsurin/pingxy-backend:latest
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    container_name: pingxy-migration
    restart: no
    environment:
      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
    networks:
      - pingxy-network
    depends_on:
      postgres:
        condition: service_healthy
    command: ["bun", "run", "db:migrate"]

  backend:
    image: vikashsurin/pingxy-backend:latest
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    container_name: pingxy-backend
    restart: always
    depends_on:
      migration:
        condition: service_completed_successfully
    environment:
      - PORT=3000
      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
    networks:
      - pingxy-network
    command: ["bun", "run", "start"]

  frontend:
    image: vikashsurin/pingxy-frontend:latest
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
    container_name: pingxy-frontend
    restart: always
    environment:
      - PORT=3001
      - ORIGIN=https://pingxy.com
      - INTERNAL_BACKEND_URL=http://backend:3000
    networks:
      - pingxy-network

  nginx:
    image: nginx:alpine
    container_name: pingxy-proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    networks:
      - pingxy-network
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/nginx/certs:ro # Mount the certs here
    depends_on:
      - frontend
      - backend

networks:
  pingxy-network:
    driver: bridge

volumes:
  postgres_data:
